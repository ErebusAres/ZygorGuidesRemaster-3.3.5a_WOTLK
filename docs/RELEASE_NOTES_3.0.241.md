# ZygorGuidesViewerRM 3.0.241

- Renames the remastered options window's `Questing` page to the established localized `Extra features` name.
- Makes the full Extra Features option table discoverable in the main Zygor options window.
- Includes the new `Show Zygor Auction House button` toggle on that page.
- Retains the same settings and the existing Blizzard Interface Options fallback without duplicating profile values.
- Extends regression coverage to verify the remastered page uses and searches the complete legacy Extra Features table.
